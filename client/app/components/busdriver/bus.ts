export interface Bus {
    id: number;
   name: string;
 driver: string;
 route: string;
status: string;
}

export interface Driver {
    id: number;
   name: string;
   lastName: string;
   status: string;
}